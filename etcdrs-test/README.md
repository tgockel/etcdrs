etcdrs-test
===========

This crate contains integration tests for the main `etcdrs` library.
Running the tests requires either `etcd` to be available on the `PATH` or you can use the environment variable
`ETCD_PROGRAM` to point to the binary.
